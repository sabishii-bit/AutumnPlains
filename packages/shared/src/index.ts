// Shared types and utilities can be exported from here
// This package can be used by both client and server

export interface GameConfig {
  maxPlayers?: number;
  tickRate?: number;
}

// Add more shared types, constants, and utilities as needed
